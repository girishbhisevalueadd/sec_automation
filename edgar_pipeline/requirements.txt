# edgartools pinned to a range known to expose the 5.x XBRL API used by
# fetcher.py (filing.xbrl().statements.income_statement().to_dataframe()).
# 5.6.x is too old; 6.x may break the API again.
edgartools>=5.30,<6.0
pandas>=2.0
openpyxl>=3.1
python-docx>=1.1
anthropic>=0.25
click>=8.1
schedule>=1.2
requests>=2.31
reportlab>=4.0
docx2pdf>=0.1.8; sys_platform == "win32"
# --- Streamlit UI layer ---
streamlit>=1.35
plotly>=5.20
streamlit-extras>=0.4
streamlit-autorefresh>=1.0
humanize>=4.0
watchdog>=4.0
